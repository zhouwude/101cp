apiVersion: apps/v1
kind: Deployment
metadata:
  name: toolbox
spec:
  replicas: 1
  selector:
    matchLabels:
      app: toolbox
  template:
    metadata:
      labels:
        app: toolbox
        access: "true"
    spec:
    # 相当于一个client 的 instance
      containers:
        - name: toolbox
          image: centos
          command: #防止退出
            - tail
            - -f
            - /dev/null